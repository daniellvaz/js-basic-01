// 01 Dado o valor abaixo crie um console.log com o valor de ativo e inativo, baseado na variável.
const isActive = false;

// 02 Crie um swit case para retornar o valor abaixo:
const myFavoriteColor = "Blue";

// 03 Crie um if ternário que dado o valor a baixo deve printar sim ou não
const isSelected = false;
